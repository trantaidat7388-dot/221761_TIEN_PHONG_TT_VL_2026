services:
  texlive:
    build:
      context: .
      dockerfile: docker/texlive/Dockerfile
    image: word2latex-texlive:latest
    working_dir: /workspace
    volumes:
      - ./outputs/pipeline_cli:/workspace
    entrypoint: ["/bin/sh", "-lc"]
    command: "xelatex --version"
